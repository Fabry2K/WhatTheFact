# Pipeline di Verifica Automatica dei Claim — Guida rapida

Questo README spiega come installare le dipendenze e in che ordine lanciare gli
script della pipeline (Sia in locale che non).

---

## 1. Prerequisiti

### 1.1 Python

```bash
python3 --version   # su Windows: py --version
```

Se non è installato: https://python.org/downloads (su Windows spunta "Add
Python to PATH" durante l'installazione).

### 1.2 Dipendenze Python

```bash
pip install requests
```

### 1.3 Ollama (Opzionale, per eseguire la Fase 2 in locale)

Installazione:

```bash
# macOS / Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows: scarica l'installer da https://ollama.com/download
```

Scarica il modello:

```bash
ollama pull qwen2.5:7b-instruct
```

Verifica che il server sia attivo (di solito parte da solo dopo l'installazione):

```bash
curl http://localhost:11434/api/tags
```

Se risponde con un JSON che elenca `qwen2.5:7b-instruct`, è tutto pronto.

### 1.4 Tavily API key (per la Fase 3 — ricerca + download pagine)

1. Registrati gratis su https://tavily.com (free tier: 1000 crediti/mese, nessuna carta richiesta)
2. Copia la tua API key (formato `tvly-xxxxx`)
3. Impostala come variabile d'ambiente **prima** di lanciare `download_sources.py` (se da eseguire in locale, altrimenti, vedi par. 1.6):

```bash
# macOS / Linux
export TAVILY_API_KEY="tvly-xxxxx"

# Windows (PowerShell)
$env:TAVILY_API_KEY="tvly-xxxxx"
```

> **Non incollare mai la chiave direttamente nel codice o in chat** — se
> l'hai già condivisa da qualche parte, rigenerala dalla dashboard Tavily.

### 1.5 Turso DB URL e authorization Token

1. Registrati gratis su https://turso.tech/ (500M letture, 10M scritture, 3G syncs, 5G storage -- 100 database generabili)
2. Crea un database (si suggerisce col nome del progetto)
3. Genera un token per accedere a quel database
4. Esporta URL del DB e auth token (se in locale, altrimenti, vedi par 1.6)

```bash
# macOS / Linux
export TURSO_DATABASE_URL="libsql://..."
export TURSO_AUTH_TOKEN="..."

# Windows (PowerShell)
$env:TURSO_DATABASE_URL="libsql://..."
$env:TURSO_AUTH_TOKEN="..."
```

### 1.6 Kaggle Notebook
Alcune fasi della pipeline vengono eseguite tramite Kaggle Notebook, così da sfruttare le risorse GPU messe a disposizione dalla piattaforma e parallelizzare le operazioni.

Per configurare correttamente l'ambiente:

1. Creare un account su Kaggle.
2. Creare un nuovo Notebook e caricare il file .ipynb relativo alla fase della pipeline da eseguire.
3. Caricare gli eventuali file di input richiesti dal notebook.
4. Inserire eventuali API key, token, URL e altre credenziali tramite Add-ons → Secrets, utilizzando gli stessi nomi dei secret attesi dal notebook.
5. Abilitare l'accesso a Internet dalle impostazioni del Notebook.
6. Abilitare l'accelerazione GPU selezionando Settings → Accelerator → GPU T4 x2, necessaria per sfruttare la parallelizzazione prevista dalla pipeline.

Una volta completata la configurazione, è possibile eseguire normalmente le celle del notebook.
---

## 2. Fase 1 — `1_generate_questions.py`

Genera domande/assertion + query di ricerca a partire dal testo sorgente
(claim breve o documento intero), usando **solo** informazioni presenti nel
testo stesso (mai conoscenza esterna del modello).

Supporta due modalità, dedotte automaticamente dall'estensione del file di
input se non specificate:

| Modalità     | Quando si usa                          | Input atteso                                   |
|--------------|-----------------------------------------|--------------------------------------------------|
| `claim`      | Claim brevi (una frase)                 | JSONL con `{"id": ..., "claim": "..."}` per riga |
| `document`   | Articoli/documenti interi               | CSV con colonne `;title;text;label` (`;` sep.)   |

### Uso

(IN LOCALE)

```bash
# Modalità claim
python3 1_generate_question/1_generate_questions.py --input Files/evaluation.csv --output Files/evaluation_with_questions.jsonl --mode claim --limit 20

# Modalità document
python3 1_generate_question/1_generate_questions.py --input Files/evaluation.csv --output Files/evaluation_with_questions.jsonl --mode document --limit 20



**Consiglio**: testa sempre prima su poche righe con `--limit`, prima di
lanciare la run completa (specialmente in modalità `document`, dove ogni
articolo può generare più chiamate al modello)

```

(SU KAGGLE NOTEBOOK)

Vedi sezione 1.6

### Argomenti principali

| Argomento | Descrizione | Default |
|---|---|---|
| `--input` | File di input (obbligatorio) | — |
| `--output` | File JSONL di output (obbligatorio) | — |
| `--mode` | `claim` o `document` | dedotto dall'estensione (`.csv` → `document`) |
| `--model` | Nome modello Ollama | `qwen2.5:7b-instruct` |
| `--limit` | Processa solo le prime N righe | tutte |
| `--max-words` | *(solo document)* dimensione chunk in parole | `1800` |
| `--overlap-words` | *(solo document)* overlap tra chunk in parole | `150` |
| `--include-text` | *(solo document)* include il testo integrale nell'output | disattivo |
| `--csv-delimiter` | *(solo document)* delimitatore del CSV | `;` |

### Output

- **Modalità claim**: per ogni claim, lista `questions` con `question`,
  `answer`, `centrality`, `provenance`, `query`.
- **Modalità document**: per ogni documento, lista `questions` con
  `assertion`, `centrality`, `query`, `provenance` (schema diverso dalla
  modalità claim: qui query e assertion sono generate in un unico step, senza
  campo `answer` separato).

---

## 3. Fase 2 — `2_download_sources.py`

Per ogni query generata in Fase 2, cerca su Tavily e scarica le prime **X**
pagine di risultato, e le salva nel database di Torso.

### Uso

(IN LOCALE)

```bash
python3 2_download_sources/2_download_sources.py --input Files/claims_with_questions.jsonl --output-dir Files/sources --num-results 5
```

(SU KAGGLE NOTEBOOK)

Vedi sezione 1.6

### Argomenti principali

| Argomento | Descrizione | Default |
|---|---|---|
| `--input` | File JSONL prodotto da `generate_questions.py` (obbligatorio) | — |
| `--turso-url` | URL DB Turso (obbligatorio) | legge da env var `TURSO_DATABASE_URL` | — |
| `--turso-token` | auth token DB Turso (obbligatorio) | legge da env var `TURSO_AUTH_TOKEN` | — |
| `--output-dir` | Cartella base dove creare le sottocartelle | `sources` |
| `--num-results` / `-x` | Numero di risultati da scaricare per query (X) | `5` |
| `--limit` | Processa solo le prime N righe del file di input | tutte |
| `--api-key` | Tavily API key | legge da env var `TAVILY_API_KEY` |
| `--search-depth` | `basic` (1 credito/query) o `advanced` (2 crediti/query) | `basic` |
| `--delay` | Pausa in secondi tra una query e l'altra | `0.5` |
| `--no-skip-existing` | Riscarica anche i claim già completati | disattivo (skip attivo) |


### Note sui crediti Tavily

Ogni chiamata di ricerca costa 1 credito in modalità `basic` (indipendentemente
da quanti risultati `X` chiedi). Con 1000 crediti/mese gratuiti e ~3-8 query
per claim/documento, puoi processare all'incirca **125-300 claim/documenti al
mese** in modalità `basic`. Se devi processare un dataset grande, valuta
`--limit` per procedere a lotti, o monitora il consumo dalla dashboard Tavily.

---

## 4. Fase 3 — `3_blind_match.py`

Fase 3 (step "Interrogazione Articoli", con classificazione diretta): per ogni claim, legge le assertion prodotte dalla Fase 1 (JSONL) e le fonti scaricate dalla Fase 2 (database Turso/libSQL, tabelle sources + claim_source_links), e per ogni coppia (assertion, articolo) chiede a un LLM locale via Ollama di classificare l'esito leggendo SOLO il testo dell'articolo.


### Uso

(IN LOCALE)

```bash
python3 3_blind_match/3_blind_matching.py --questions Files/claims_with_questions.jsonl --sources-dir Files/sources --output-dir Files/matching_results
```

(SU KAGGLE NOTEBOOK)

Vedi sezione 1.6

### Argomenti principali

| Parameter | Default | Description |
|---|---|---|
| `--questions` | required | JSONL file containing the questions/assertions generated in Phase 2 |
| `--turso-url` | `TURSO_DATABASE_URL` | URL of the Turso database containing the sources downloaded in Phase 3 |
| `--turso-token` | `TURSO_AUTH_TOKEN` | Authentication token for the Turso database |
| `--output-dir` | `matching_results` | Directory where matching results are saved |
| `--model` | `qwen2.5:7b-instruct` | Ollama model used for the blind matching |
| `--ollama-url` | `http://localhost:11434/api/chat` | Ollama chat endpoint |
| `--ollama-urls` | `None` | Comma-separated list of Ollama endpoints, used to distribute requests across multiple GPUs |
| `--workers` | implementation default | Number of concurrent Ollama requests |
| `--timeout` | `300` | Maximum time in seconds allowed for each Ollama request |
| `--keep-alive` | implementation default | How long Ollama keeps the model loaded in memory |
| `--docs-per-question` | implementation default | Maximum number of documents evaluated for each question |
| `--mapping` | `pooled` | Document selection strategy: `pooled` considers all documents for the claim, while `strict` only considers documents retrieved by the corresponding query |
| `--min-content-chars` | `500` | Minimum document length; shorter documents are discarded |
| `--limit` | `None` | Process only the first N claims |
| `--delay` | `0` | Delay in seconds between consecutive Ollama calls |
| `--time-budget-minutes` | `None` | Optional maximum execution time; already processed claims are skipped when the script is resumed |

---
## 5. Esempio di run end-to-end (IN LOCALE)

- Caricare i dataset di input (claims/documents) nella cartella Files

```bash
export TAVILY_API_KEY="tvly-xxxxx"
export TURSO_DATABASE_URL="libsql://..."
export TURSO_AUTH_TOKEN="..."

# Fase 1
python3 1_generate_question/1_generate_questions.py --input Files/evaluation.csv --output Files/evaluation_with_questions.jsonl --mode claim --limit 20

# Fase 2 (usa l'output della Fase 1 come input)
python3 2_download_sources/2_download_sources.py --input Files/claims_with_questions.jsonl --output-dir Files/sources --num-results 5

# Fase 3
python3 3_blind_match/3_blind_matching.py --questions Files/claims_with_questions.jsonl --sources-dir Files/sources --output-dir Files/matching_results

# Evaluation
python3 4_evaluate.py --matching-dir Files/matching_results --labels Files/claims_labelled.jsonl
```

---

## 5. Problemi comuni

- **`python: command not found`** → usa `python3` (macOS/Linux) o `py` (Windows). Vedi sezione 1.1.
- **`usage: ... error: the following arguments are required: --output`** → probabilmente hai una versione vecchia/sbagliata del file locale; riscarica lo script e sovrascrivi quello esistente.
- **Ollama non risponde / connection refused** → verifica che sia in esecuzione con `curl http://localhost:11434/api/tags`; se non risponde, avvialo (di solito parte da solo dopo l'installazione, altrimenti lancia `ollama serve`).
- **`ERRORE: nessuna API key fornita`** (Fase 3) → imposta `TAVILY_API_KEY` come da sezione 1.4, oppure passa `--api-key`.
- **Il modello restituisce output JSON malformato ripetutamente** → prova a controllare i log `[DEBUG] raw model content` (se presenti) per vedere cosa genera davvero il modello, spesso segnala un disallineamento tra il prompt e il formato atteso dal parsing.